#!/usr/bin/env ruby
# vi:syntax=ruby
require 'yaml'

build_path = ARGV[0]

puts YAML.dump({
  'config_vars' => {},
  'default_process_types' => {
    'web' => "bash -c 'export HOME=#{build_path} ; source #{build_path}/ldconfig_wrapper.sh ; #{build_path}/vendor/dotnet/dotnet run'"
  }
})
